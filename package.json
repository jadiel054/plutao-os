{
  "name": "plutao",
  "version": "0.1.0",
  "private": true,
  "description": "Plutão — Personal Autonomous AI Operating System",
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "start": "npm run start -w apps/web",
    "lint": "npm run lint -w apps/web",
    "typecheck": "npm run typecheck -w packages/domain --if-present && npm run typecheck -w packages/db --if-present"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  }
}
